export enum WalletType {
  Cashback = "cashback",
  Points = "points",
}

export enum PointTransactionType {
  Blocked = "blocked",
  Earned = "earned",
  Expired = "expired",
  Redeemed = "redeemed",
}
