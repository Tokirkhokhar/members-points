/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.pexels.com",
      "images.unsplash.com",
      "imagedelivery.net",
      "localhost",
      "api.memberspoint.com",
    ],
  },
};

export default nextConfig;
